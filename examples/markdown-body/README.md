# Markdown Body Example

This example shows how to render a markdown document inside a normal email shell.

```bash
npm install chakra-email react react-dom react-markdown
```

See [markdown-email.tsx](markdown-email.tsx).

The repository-level `npm run examples:check` command compiles this source and
invokes its render function as part of the examples smoke suite.
