# Basic Email Example

This example renders a complete welcome email using `chakra-email`.

```bash
npm install chakra-email react react-dom
```

See [welcome-email.tsx](welcome-email.tsx).

The repository-level `npm run examples:check` command compiles this source and
invokes its render function as part of the examples smoke suite.
